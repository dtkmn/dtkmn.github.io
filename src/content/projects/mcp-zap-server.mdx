---
title: MCP ZAP Server
summary: Secure OWASP ZAP scanning exposed as an MCP server for AI agents, with reusable tool-governance boundaries, CI security gates, observability, and production-oriented auth.
date: 2025-04-20
draft: false
featured: true
detailPage: true
tier: flagship
status: active
order: 1
repoUrl: https://github.com/dtkmn/mcp-zap-server
docsUrl: https://dtkmn.github.io/mcp-zap-server/
demoUrl: https://dtkmn.github.io/mcp-zap-server/demo.html
heroImage: /images/projects/mcp-zap-server-hero.svg
relatedPosts:
  - demos-are-cheap-production-mcp-is-expensive
  - mcp-the-usb-c-of-ai-integrations
tags:
  - AI systems
  - MCP
  - Spring Boot
  - OWASP ZAP
  - Security tooling
stack:
  - Java 25
  - Spring Boot 4
  - MCP
  - OWASP ZAP
  - Docker Compose
  - Helm
audience:
  - AppSec teams that want AI-assisted scanning workflows
  - Platform engineers integrating security checks into developer tooling
  - Teams evaluating MCP beyond toy demos
proofPoints:
  - "v0.10.1 is a drop-in compatibility patch: Streamable HTTP responses to server-initiated requests now reach the MCP runtime instead of being rejected as methodless requests."
  - 58 GitHub stars and 8 forks as of June 30, 2026.
  - "PRs #167 and #168 consume MCP Gateway Core 0.7.2, align runtime and delivery metadata, and leave tool schemas, auth settings, Helm values, and the database contract unchanged."
  - Supports API-key and JWT authentication modes, tool scopes, runtime policy bundles, URL validation, rate limits, workspace quotas, and audit events.
  - "The support table marks >= 0.10.1 active and 0.10.0 upgrade-only for Streamable HTTP client compatibility."
evidenceStats:
  - label: Public traction
    value: 58 stars
    detail: GitHub stars as recorded on June 30, 2026, with 8 forks.
    icon: github
  - label: Release track
    value: "0.10.1"
    detail: July 16 compatibility patch for Streamable HTTP responses to server-initiated requests.
    icon: rocket
  - label: Guardrails
    value: Gateway
    detail: Tool scopes, policy enforcement, abuse protection, and audit boundaries use the 0.7.2 shared Java governance adapters.
    icon: shield
  - label: Delivery paths
    value: CI + Helm
    detail: GitHub/GitLab security-gate examples, Docker Compose, Helm, and docs are all part of the artifact.
    icon: check-circle
  - label: Compatibility fix
    value: HTTP 202
    detail: Cursor keepalive replies now pass through for downstream session correlation instead of failing request-shape validation.
    icon: terminal
evidenceAssets:
  - type: repository
    label: Repository signal
    title: Public code with visible adoption and maintenance pressure
    detail: The repo has public traction, a current compatibility patch, and enough delivery and operator material to inspect beyond a README headline.
    href: https://github.com/dtkmn/mcp-zap-server
    hrefLabel: Inspect repository
  - type: governance
    label: Release evidence
    title: v0.10.1 Streamable HTTP compatibility patch
    detail: The release updates both gateway dependencies to 0.7.2, restores response pass-through for server-initiated requests, and aligns runtime, image, Helm, Registry, extension, and install metadata.
    href: https://github.com/dtkmn/mcp-zap-server/releases/tag/v0.10.1
    hrefLabel: Read release notes
  - type: docs
    label: Docs surface
    title: Separate documentation and operator runbooks
    detail: The project has docs for auth, tool surfaces, abuse protection, observability, scan execution, runtime policy bundles, and production checklists.
    href: https://dtkmn.github.io/mcp-zap-server/
    hrefLabel: Read docs
  - type: demo
    label: Demo path
    title: Agent-to-scanner flow is visible
    detail: The hosted demo gives the project a concrete interaction path instead of asking visitors to imagine the workflow.
    href: https://dtkmn.github.io/mcp-zap-server/demo.html
    hrefLabel: Open demo
  - type: security
    label: Operational guardrails
    title: Auth, policy, audit, rate limits, and URL boundaries
    detail: This is the proof that the project is thinking about operator risk, live remediation pressure, and failure handling, not just successful tool calls.
---

## What it is

This project turns OWASP ZAP into an MCP-accessible security tool so an AI agent can import API
schemas, run scanning workflows, and pull structured findings without falling back to brittle shell
scripts.

The reusable boundary layer now has its own site entry: [MCP Gateway Core](/projects/mcp-gateway-core/)
is the Java contract layer for tool identity, authorization, policy, audit, abuse protection, and
adapter behavior. This project stays responsible for the ZAP-specific runtime.

## Why it matters

Many MCP demos stop at convenience. This one is stronger because it sits on a real operational
boundary: security automation. The interesting question is not whether an agent can call a tool. It
is whether that tool can be exposed in a way that is safe enough, observable enough, and packaged
well enough to fit into actual engineering workflows.

## What makes it credible

The repository has enough surface area to show that it has moved past novelty:

- a documentation site with security, deployment, observability, and operator runbooks,
- a demo path that shows the workflow end to end,
- release notes and changelog discipline,
- GitHub Actions and GitLab security-gate examples,
- Docker Compose and Helm deployment material,
- API-key/JWT auth, tool scopes, runtime policy bundles, audit events, rate limits, workspace quotas,
  and target URL validation.

The June 10 v0.9.0 release made the proof sharper: policy, audit, and abuse-protection handling moved
through shared [MCP Gateway Core](/projects/mcp-gateway-core/) adapter boundaries instead of one-off
local filters. The security-gate examples rejected mutable image tags, and the release notes made
validation commands inspectable.

The July 16 v0.10.1 release is narrower than a feature launch and more useful as evidence. A
Streamable HTTP endpoint receives requests, notifications, and responses on the same `POST` route.
The previous gateway adapter treated a response-shaped envelope as a request, then rejected it for
having an `id` and `result` or `error` but no `method`. Cursor keepalive replies consequently failed
instead of reaching Spring AI for session correlation.

[PR #167](https://github.com/dtkmn/mcp-zap-server/pull/167) moved both gateway dependencies to
`0.7.2`. [PR #168](https://github.com/dtkmn/mcp-zap-server/pull/168) packaged that correction as
v0.10.1 and aligned runtime, OCI image, Helm chart, MCP Registry, extension-proof, and install
metadata. Response envelopes now pass through and complete with HTTP `202`; requests that carry a
`method` still take the existing authorization and abuse-protection path. The body, session id,
custom headers, body-size limit, and surrounding authentication chain remain intact.

This is a drop-in patch from v0.10.0, not an excuse to imply a larger redesign. Tool schemas,
authentication settings, Helm values, and the database contract are unchanged. The release also
expands CodeQL coverage and pins policy dry-run regression coverage, while the support table marks
`>= 0.10.1` active and `0.10.0` upgrade-only for this client-compatibility failure. That is what
production-adjacent maintenance looks like: a real downstream failure gets a bounded fix, a release,
and an explicit upgrade path.

That combination is exactly why this deserves a page here. The repository handles setup and usage.
This page explains why the project matters in the first place.

## How it connects to the writing

This is the practical counterpart to the production argument in [Demos Are Cheap. Production MCP Is
Expensive.](/posts/demos-are-cheap-production-mcp-is-expensive/), the protocol explainer in
[MCP: the USB-C of AI integrations](/posts/mcp-the-usb-c-of-ai-integrations/), and the reusable
boundary layer in [MCP Gateway Core](/projects/mcp-gateway-core/).

The protocol article explains why a common integration layer matters. The production article explains
why the protocol is not enough. This project shows what happens when the tool on the other side has
real security, deployment, observability, and remediation pressure.
